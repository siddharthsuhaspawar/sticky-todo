[package]
name = "three-lists"
version = "1.0.0"
description = "Three Lists — keep only three lists"
authors = ["Three Lists"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
base64 = "0.22"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
panic = "abort"
